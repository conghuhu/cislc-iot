package com.cislc.shadow.manage.device.repository;

import com.cislc.shadow.manage.device.entity.SduGate;
import java.lang.Integer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SduGateRepository extends JpaRepository<SduGate, Integer> {
}
